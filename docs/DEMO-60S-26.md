# ATTEST — sixty-second version

For a submission platform with a hard limit. Same product, same clicks, no new
screens. Four beats.

---

## 0:00 — cold open · 0 clicks

> *"Fifty-three lakh entered. Three hundred and fifty-three rupees came out."*

Point at the spine, not the numbers. `SOURCE` and `MATCHING` run full width;
`VERIFICATION` is a stub carrying **₹48,03,127.81 held**; `POLICY` and `ACTION`
are hairlines.

> *"And the work is ranked by what it unlocks: one missing field is holding
> ₹47,96,811.78 across 197 settlements. ATTEST cannot supply that field, so it
> offers no button — it says REQUIRES EXTERNAL EVIDENCE."*

## 0:20 — one case · 2 clicks

Click `000089` in *Needs a person*, then **Open ↗**.

```
₹7,292.03   DISPUTED
₹97,759.84  AGREED
```

> *"Ninety-seven thousand is settled whichever explanation is right. Seven
> thousand turns on which one is."*

## 0:35 — the core idea · 1 click

Investigate.

```
◇ MODEL    proposed    capture-batch
○ SOLVER   tested      NON DISCRIMINATIVE
● ENGINE   abstain     ABSTAINED
```

> *"The model proposed an anchor. The solver found it in all four explanations,
> so it separates nothing. The engine abstained."*

**VERDICT UNCHANGED · NO FINANCIAL ACTION**

## 0:50 — the consequence · 2 clicks

Policy, then Trust.

```
UNPRICED · REVIEW
Nothing was priced. The proof did not establish a unique explanation,
so there is no error probability to multiply.
```

```
Live Razorpay validation
NOT VERIFIED
```

> *"No confidence score, because there is nothing to be confident about. And the
> system's own Trust screen leads with what it has not verified — no live
> Razorpay account has ever been contacted."*

## 1:00 — close

> *"ATTEST never pretends to know more than the evidence proves."*

---

**Five clicks. Nothing staged.** If time runs short, cut the Policy beat before
the Trust beat — the limitation is the more memorable half.
